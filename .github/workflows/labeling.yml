name: labeling

on:
  - pull_request_target

jobs:
  use-labeler:
    permissions:
      contents: read
      pull-requests: write
    runs-on: ubuntu-22.04
    steps:
      - name: use-labeler
        uses: actions/labeler@v5
        with: 
          configuration-path: "./labeler-config.yml"
